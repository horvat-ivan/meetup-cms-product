{
  "name": "meetup-cms-product",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "test": "node --test --test-reporter=spec '.github/workflow-scripts/**/*.test.mjs'"
  },
  "dependencies": {
    "gray-matter": "^4.0.3"
  }
}
